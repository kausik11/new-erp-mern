
---

### **2. Backend README (`backend/README.md`)**

```markdown
# ERP Backend

This is the backend of the ERP system built with **Node.js**, **Express.js**, and **MongoDB**.

## Prerequisites

- Node.js >= 18.x
- npm
- MongoDB instance

## Installation

1. Clone the repository:
   ```bash
   git clone <your-repo-url>
   cd backend
   npm install
   yarn dev
